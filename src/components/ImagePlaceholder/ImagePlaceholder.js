import React from 'react';

import classes from './ImagePlaceholer.module.css';

export const ImagePlaceholder = () => (
  <div className={classes.container}>Image generation...</div>
);

ImagePlaceholder.propTypes = {};
